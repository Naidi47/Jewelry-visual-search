# IMPORTANT: This is a TEMPLATE. Do NOT commit with real values.
# Create the actual secret with:
#   kubectl create secret generic jewelry-search-secrets \
#     --from-literal=mongodb-uri="YOUR_MONGODB_URI" \
#     -n jewelry-search

apiVersion: v1
kind: Secret
metadata:
  name: jewelry-search-secrets
  namespace: jewelry-search
type: Opaque
stringData:
  mongodb-uri: "REPLACE_WITH_YOUR_ATLAS_CONNECTION_STRING"
